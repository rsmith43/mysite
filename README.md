# Website Management Guide

**SITE** [INSERT LINK]

### Workflow
1.  Find `index.html` file
2.  Click the **Pencil Icon** (top right of the file view).
3.  Scroll to the bottom and click the green **Commit changes** button.
4.  Wait 60 seconds, then Hard Refresh site (`Ctrl+F5` on Windows, `Cmd+Shift+R` on Mac).
